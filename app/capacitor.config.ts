import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.debby.asistentepsicopedagogico',
  appName: 'Asistente Psicopedagógico',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
